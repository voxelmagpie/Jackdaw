//! 3 # 3

import "@stlib/memory"

@Unsafe
struct X {
    a: *X,
    b: I32
}

fn start () unsafe {
    var p = alloc[X](1);
    p.* = X{ a=nullptr, b = readI32() };
    p.*.a = p;
    writeI32(p.*.a.*.a.*.a.*.b);
}
