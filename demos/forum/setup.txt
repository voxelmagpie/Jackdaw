sudo apt install mariadb-server mariadb-client libmariadb-dev
sudo service mariadb start
#sudo mysql_secure_installation # Not needed in newer versions
sudo mariadb < forum.sql
sudo mariadb forum < test_data.sql
sudo mariadb
	CREATE USER 'USER'@localhost IDENTIFIED BY 'PASSWORD';
	GRANT ALL PRIVILEGES ON forum.* TO 'USER'@'localhost';
	FLUSH PRIVILEGES;

Create 'user' and 'pw' files in the same directory as this setup.txt file
containing the database username and password respectively.