//! 3 1 # 1
//! 0 # 8 

fn start () {
    var i: I32;
    var j: X;

    i = readI32();

    if i > 0 {
        j = X{ a = readI32() };
    }
    else {
        j = X{ a=8 };
    }
}

struct X {
    a: I32

    fn onDrop (ref self: Self) {
        writeI32(self.a);
    }
}
