//! 4 8 5 1 2 # 8\n5

import "@stlib/raw_slice"

fn start () unsafe {
    var x = [readI32(), readI32(), readI32()];
    var r = x.asRawSlice();
    var r = r.subSlice(readI32(), readI32());

    foreach x in r.iter() {
        writeI32(x.*);
    }

}
